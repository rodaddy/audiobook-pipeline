## Summary

What does this PR do and why?

## Changes

- Change 1
- Change 2

## Testing

How did you test these changes?

```bash
# Example test commands
bin/audiobook-convert --dry-run --verbose /path/to/test-input/
shellcheck -x lib/your-changed-file.sh
```

## Checklist

- [ ] Tested with `--dry-run` and `--verbose`
- [ ] Tested on target platform (macOS / Linux)
- [ ] `shellcheck` passes on modified `.sh` files
- [ ] No secrets or personal paths in committed files
- [ ] README updated if adding new config options
- [ ] `config.env.example` updated if adding new variables
